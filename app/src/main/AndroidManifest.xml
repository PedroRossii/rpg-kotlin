<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.RPG_front">

        <!-- Activity principal / launcher -->
        <activity
            android:name="com.example.rpg_front.view.CriacaoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Outras activities do app -->
        <activity
            android:name="com.example.rpg_front.view.FichaActivity"
            android:exported="false" />

        <!-- Adicione aqui outras activities, sempre definindo android:exported -->
        <!-- Exemplo:
        <activity
            android:name=".view.OutraActivity"
            android:exported="false" />
        -->

    </application>

</manifest>
